<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

  <PreferenceCategory app:title="@string/assistant">
    <SwitchPreferenceCompat
      app:key="voice_enabled"
      app:title="Utilizar voz para instruções" />
  </PreferenceCategory>

  <PreferenceCategory app:title="@string/barcode_reader">
    <SwitchPreferenceCompat
      app:key="barcode_input_camera_open"
      app:summaryOff="Os campos de leitura de códigos devem ser digitados ou lidos por leitor bluetooth"
      app:summaryOn="Abre automaticamente os campos de leitura de código de barra"
      app:title="Utilizar câmera por padrão" />
  </PreferenceCategory>

  <!--<SwitchPreferenceCompat
    app:dependency="sync"
    app:key="attachment"
    app:summaryOff="@string/attachment_summary_off"
    app:summaryOn="@string/attachment_summary_on"
    app:title="@string/attachment_title" />
-->

</PreferenceScreen>
